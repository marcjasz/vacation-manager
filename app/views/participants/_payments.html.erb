<table>
<thead>
    <tr>
    <th>Date</th>
    <th>Amount</th>
    </tr>
</thead>

<tbody>
    <% Participation.find_by(participant: @participant, group: group).payments.each do |payment| %>
    <tr>
        <td><%= payment.date %></td>
        <td><%= payment.amount %></td>
    </tr>
    <% end %>
</tbody>
</table>