<table>
<thead>
    <tr>
    <th>Date</th>
    <th>Amount</th>
    <th><%= render 'payment_form', action: 'new', participation: Participation.find_by(participant: @participant, group: group) %></th>
    </tr>
</thead>

<tbody>
    <% Participation.find_by(participant: @participant, group: group).payments.each do |payment| %>
    <tr>
        <td><%= payment.date %></td>
        <td><%= payment.amount %></td>
    </tr>
    <% end %>
</tbody>
</table>