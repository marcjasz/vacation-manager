<p id="notice"><%= notice %></p>

<h1>Counselors</h1>

<table>
  <thead>
    <tr>
      <th>Pesel</th>
      <th>Name</th>
      <th>Surname</th>
      <th>Address</th>
      <th>Phone number</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @counselors.each do |counselor| %>
      <tr>
        <td><%= counselor.pesel %></td>
        <td><%= counselor.name %></td>
        <td><%= counselor.surname %></td>
        <td><%= counselor.address %></td>
        <td><%= counselor.phone_number %></td>
        <td><%= link_to 'Show', counselor %></td>
        <td><%= link_to 'Edit', edit_counselor_path(counselor) %></td>
        <td><%= link_to 'Destroy', counselor, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Counselor', new_counselor_path %>
